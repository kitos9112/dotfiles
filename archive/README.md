# Archive

Keep deprecated or retired repo bits here, outside `home/`, so they remain visible in git history without being rendered by chezmoi.

Use one subdirectory per retired tool or feature and add a short note explaining what was removed and why.
